# Shared build context: each Containerfile COPY's its own subset.
# Do not ignore api/, internal/, pkg/, or cmd/ — different images need different trees.
bin/
.git
cover.out
coverage.html
docs/
openspec/
.github/
*.md
